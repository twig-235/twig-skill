---
name: game-character-art
description: Create a consistent game character set containing full, chat, grid, and icon art.
---

# Game character art for Claude

Read `../../spec.md` before starting. Treat it as the required output contract and use `../../references/` only as visual references. Use the available image-generation capability for artwork and preserve the approved full illustration's identity in every derivative.

Perform alpha, framing, and grid validation with available local tools. Leave exactly `full.png`, `chat.png`, `grid.png`, and `icon.png` in the final character folder.
